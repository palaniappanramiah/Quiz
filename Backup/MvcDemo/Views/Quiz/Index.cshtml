@{
    ViewBag.Title = "Index";
}

@if (Request.IsAuthenticated)
{
    <h2>Index</h2>
    <p>
        @Html.ActionLink("Create New", "CreateQuiz")
    </p>
}


else
{
    <h2>To Continue</h2>
     @Html.ActionLink( "Log on", "LogOn", "Account")
}